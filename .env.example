# Optional. If unset, the app generates data/.master.key on first run.
# 32-byte AES-256 key as 64 hex characters.
SETTINGS_ENCRYPTION_KEY=

# Where encrypted settings are stored. Default: ./data
DATA_DIR=
