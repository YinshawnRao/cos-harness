# Optional. If unset, the app generates .master.key on first run
# (desktop: OS userData/data; otherwise ./data).
# 32-byte AES-256 key as 64 hex characters.
SETTINGS_ENCRYPTION_KEY=

# Override encrypted settings directory.
# Desktop app sets this to "<userData>/data" automatically.
DATA_DIR=
